version: "3.9"
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      NEXT_TELEMETRY_DISABLED: "1"
      MFE_DEBUG: "1"
    ports:
      - "3000:3000"
      - "3001:3001"
    restart: unless-stopped
    command: ["pnpm", "dev"]